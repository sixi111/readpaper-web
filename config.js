// readpaper config (static deploy)
// Set this to your deployed Cloudflare Worker URL, e.g. "https://readpaper-worker.yourname.workers.dev"
// Leave empty to make the page show a config error.
window.READPAPER_CONFIG = {
  WORKER_URL: "https://readpaper-worker.sixian.workers.dev"
};
